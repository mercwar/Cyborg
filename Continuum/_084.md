{
  "INCINTUUM_JVM_BRIDGE": {
    "VERSION": "1.0",
    "MODULE": "INCINTUUM_JVM_BRIDGE.ijvb",
    "ROLE": "Execution bridge between CBORG dispatch packets and JVM runtime",
    "DESCRIPTION": "Receives dispatch packets from INCINTUUM Gateway Router, invokes Java classes/methods emitted by CYBORD, captures output, and updates continuum state.",

    "INPUT": "dispatch",
    "OUTPUT": "jvm_runtime_result",

    "RUNTIME_MAP": {
      "JAVA_METHOD": {
        "FORMAT": {
          "class": "string",
          "method": "string",
          "args": "array"
        },
        "DESC": "Direct invocation of CYBORD‑emitted Java methods"
      },
      "JAVA_STATIC": {
        "FORMAT": {
          "class": "string",
          "static_method": "string",
          "args": "array"
        },
        "DESC": "Invocation of static Java methods"
      },
      "JAVA_MAIN": {
        "FORMAT": {
          "class": "string"
        },
        "DESC": "Execution of Java main entrypoint"
      }
    },

    "EXECUTION_LOGIC": {
      "DESCRIPTION": "Determines how dispatch packets map to JVM runtime calls.",
      "RULES": [
        "IF dispatch.target == 'java.method' → JAVA_METHOD",
        "IF dispatch.target == 'java.static' → JAVA_STATIC",
        "IF dispatch.target == 'java.main' → JAVA_MAIN"
      ]
    },

    "HANDLER": {
      "jvm_runtime_handler": {
        "INPUT": "dispatch",
        "ACTIONS": [
          "validate dispatch packet",
          "select JVM runtime mode",
          "invoke JVM runtime",
          "capture stdout/stderr",
          "serialize output",
          "update continuum_state.managed.java"
        ],
        "OUTPUT": "jvm_runtime_result"
      }
    },

    "RESULT_FORMAT": {
      "FIELDS": {
        "timestamp": "iso8601",
        "mode": "method | static | main",
        "output": "mixed",
        "errors": "array"
      }
    },

    "ERRORS": {
      "INVALID_DISPATCH": "Dispatch packet missing required fields",
      "UNKNOWN_RUNTIME_MODE": "Dispatch target not mapped to JVM runtime",
      "JVM_EXEC_FAILURE": "JVM runtime threw an exception",
      "STATE_UPDATE_FAILURE": "Failed to update continuum state"
    },

    "META": {
      "CONTINUUM": "CBORD → CYBORD → CBORG → INCINTUUM",
      "LANGUAGES": [
        "C",
        "CPP",
        "JAVA",
        "ASM",
        "PHP"
      ]
    }
  }
}

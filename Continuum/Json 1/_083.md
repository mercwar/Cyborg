{
  "INCINTUUM_PHP_RUNTIME_BRIDGE": {
    "VERSION": "1.0",
    "MODULE": "INCINTUUM_PHP_RUNTIME_BRIDGE.iprb",
    "ROLE": "Execution bridge between CBORG dispatch packets and PHP runtime functions",
    "DESCRIPTION": "Receives dispatch packets from INCINTUUM Gateway Router, invokes PHP functions emitted by CYBORD, captures output, and updates continuum state.",

    "INPUT": "dispatch",
    "OUTPUT": "php_runtime_result",

    "RUNTIME_MAP": {
      "FUNCTION_CALL": {
        "FORMAT": {
          "fn": "string",
          "args": "array",
          "context": "object"
        },
        "DESC": "Direct invocation of CYBORD‑emitted PHP functions"
      },
      "CLASS_METHOD": {
        "FORMAT": {
          "class": "string",
          "method": "string",
          "args": "array"
        },
        "DESC": "Invocation of CYBORD‑emitted PHP classes/methods"
      },
      "MAIN_ENTRY": {
        "FORMAT": {
          "entry": "string"
        },
        "DESC": "Execution of PHP main entrypoint"
      }
    },

    "EXECUTION_LOGIC": {
      "DESCRIPTION": "Determines how dispatch packets map to PHP runtime calls.",
      "RULES": [
        "IF dispatch.target == 'php.function' → invoke FUNCTION_CALL",
        "IF dispatch.target == 'php.class' → invoke CLASS_METHOD",
        "IF dispatch.target == 'php.main' → invoke MAIN_ENTRY"
      ]
    },

    "HANDLER": {
      "php_runtime_handler": {
        "INPUT": "dispatch",
        "ACTIONS": [
          "validate dispatch packet",
          "select runtime mode (function/class/main)",
          "invoke PHP runtime",
          "capture stdout/stderr",
          "serialize output",
          "update continuum_state.managed.php"
        ],
        "OUTPUT": "php_runtime_result"
      }
    },

    "RESULT_FORMAT": {
      "FIELDS": {
        "timestamp": "iso8601",
        "mode": "function | class | main",
        "output": "mixed",
        "errors": "array"
      }
    },

    "ERRORS": {
      "INVALID_DISPATCH": "Dispatch packet missing required fields",
      "UNKNOWN_RUNTIME_MODE": "Dispatch target not mapped to PHP runtime",
      "PHP_EXEC_FAILURE": "PHP runtime threw an exception",
      "STATE_UPDATE_FAILURE": "Failed to update continuum state"
    },

    "META": {
      "CONTINUUM": "CBORD → CYBORD → CBORG → INCINTUUM",
      "LANGUAGES": [
        "C",
        "CPP",
        "JAVA",
        "ASM",
        "PHP"
      ]
    }
  }
}

{
  "CBORD_COMPILER_FILES": {
    "CBORD_PARSER.cbp": "/* AIFVS-ARTIFACT */\n/* FILE: CBORD_PARSER.cbp */\n\nPARSER {\n    TOKENS {\n        KEYWORDS: [CBORD, BEGIN, END, IF, WHILE, FOR, RETURN, ASM]\n        TYPES: [int, float, char, void]\n        SYMBOLS: [+, -, *, /, =, ==, !=, <, >, {, }, (, ), ;]\n    }\n\n    RULE program {\n        CBORD BEGIN\n        statements*\n        CBORD END\n    }\n\n    RULE statement {\n        decl | assign | call | if | while | for | return | asm_inline\n    }\n\n    RULE decl {\n        type IDENT \"=\" expr \";\"\n    }\n\n    RULE assign {\n        IDENT \"=\" expr \";\"\n    }\n\n    RULE call {\n        IDENT \"(\" args \")\" \";\"\n    }\n\n    RULE if {\n        IF \"(\" expr \")\" block (ELSE block)?\n    }\n\n    RULE while {\n        WHILE \"(\" expr \")\" block\n    }\n\n    RULE for {\n        FOR \"(\" init \";\" cond \";\" step \")\" block\n    }\n\n    RULE return {\n        RETURN expr \";\"\n    }\n\n    RULE asm_inline {\n        ASM \"{\" TEXT \"}\"\n    }\n}\n",

    "CBORD_IR.json": "{\n  \"CBORD_IR\": {\n    \"NODES\": [\n      \"MODULE\", \"FUNCTION\", \"BLOCK\", \"PHI\",\n      \"DECL\", \"ASSIGN\", \"CALL\", \"RETURN\",\n      \"IF\", \"WHILE\", \"FOR\", \"BREAK\", \"CONTINUE\",\n      \"BIN_OP\", \"UN_OP\", \"LOAD\", \"STORE\",\n      \"CONST\", \"VAR\", \"ADDR\", \"DEREF\",\n      \"ASM_INLINE\", \"EMIT_C\"\n    ]\n  }\n}\n",

    "CBORD_LOWERING_RULES.irf": "/* AIFVS-ARTIFACT */\n/* FILE: CBORD_LOWERING_RULES.irf */\n\nCBORD_LOWERING_RULES\n{\n    LOWER BIN_OP {\n        +  → C: (left + right), ASM: add\n        -  → C: (left - right), ASM: sub\n        *  → C: (left * right), ASM: imul\n        /  → C: (left / right), ASM: idiv\n        == → C: (left == right), ASM: cmp + je\n        != → C: (left != right), ASM: cmp + jne\n    }\n\n    LOWER IF {\n        C: if(cond) { then } else { else }\n        ASM:\n            cmp cond\n            je THEN_LABEL\n            jmp ELSE_LABEL\n    }\n\n    LOWER WHILE {\n        C: while(cond) { body }\n        ASM:\n            LOOP_START:\n                cmp cond\n                jne LOOP_END\n                body\n                jmp LOOP_START\n            LOOP_END:\n    }\n\n    LOWER FUNCTION {\n        C: standard C function\n        ASM:\n            push rbp\n            mov rbp, rsp\n            ...body...\n            pop rbp\n            ret\n    }\n\n    LOWER ASM_INLINE {\n        C: __asm__(text)\n        ASM: text\n    }\n\n    LOWER EMIT_C {\n        C: c_text\n        ASM: (none)\n    }\n}\n",

    "CBORD_ASM_EMITTER.abf": "/* AIFVS-ARTIFACT */\n/* FILE: CBORD_ASM_EMITTER.abf */\n\nCBORD_ASM_EMITTER\n{\n    TARGET: x86_64_WIN64\n\n    EMIT FUNCTION_BEGIN(name) {\n        print(\"global \" + name)\n        print(name + \":\")\n        print(\"    push rbp\")\n        print(\"    mov rbp, rsp\")\n    }\n\n    EMIT FUNCTION_END {\n        print(\"    pop rbp\")\n        print(\"    ret\")\n    }\n\n    EMIT BIN_OP(op, left, right) {\n        switch(op) {\n            case \"+\": print(\"mov rax, \" + left)\n                      print(\"add rax, \" + right)\n                      break\n\n            case \"-\": print(\"mov rax, \" + left)\n                      print(\"sub rax, \" + right)\n                      break\n\n            case \"*\": print(\"mov rax, \" + left)\n                      print(\"imul rax, \" + right)\n                      break\n\n            case \"/\": print(\"mov rax, \" + left)\n                      print(\"cqo\")\n                      print(\"idiv \" + right)\n                      break\n        }\n    }\n\n    EMIT LOAD(addr) {\n        print(\"mov rax, [\" + addr + \"]\")\n    }\n\n    EMIT STORE(addr, value) {\n        print(\"mov [\" + addr + \"], \" + value)\n    }\n\n    EMIT RETURN(expr) {\n        print(\"mov rax, \" + expr)\n        print(\"pop rbp\")\n        print(\"ret\")\n    }\n}\n"
  }
}

// ============================================================
// CYBORD CORE (patched for STARFIRE)
// ============================================================

const CYBORD = (() => {
  const state = {
    bus_events: [],
    last_update: null
  };

  const now = () => new Date().toISOString();

  function dispatch(type, payload = {}) {
    const evt = { layer: "CYBORD", type, payload, timestamp: now() };
    state.bus_events.push(evt);
    state.last_update = now();
    return evt;
  }

  // STARFIRE bridge
  function toContinuum(type, payload = {}) {
    return {
      source_layer: "CYBORD",
      event_type: type,
      payload,
      timestamp: now()
    };
  }

  return {
    dispatch,
    toContinuum,
    getState: () => JSON.parse(JSON.stringify(state))
  };
})();

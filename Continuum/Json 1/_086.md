{
  "INCINTUUM_NATIVE_GATEWAY": {
    "VERSION": "1.0",
    "MODULE": "INCINTUUM_NATIVE_GATEWAY.ing",
    "ROLE": "Execution gateway for CBORD-native binaries inside INCINTUUM",
    "DESCRIPTION": "Receives dispatch packets from INCINTUUM Gateway Router, invokes native CBORD-linked binaries, captures output, and updates continuum state.",

    "INPUT": "dispatch",
    "OUTPUT": "native_runtime_result",

    "RUNTIME_MAP": {
      "NATIVE_EXEC": {
        "FORMAT": {
          "binary": "string",
          "args": "array",
          "env": "object"
        },
        "DESC": "Direct execution of CBORD-linked native binaries"
      },
      "NATIVE_MAIN": {
        "FORMAT": {
          "binary": "string"
        },
        "DESC": "Execution of main entrypoint for native binaries"
      }
    },

    "EXECUTION_LOGIC": {
      "DESCRIPTION": "Maps dispatch packets to native execution modes.",
      "RULES": [
        "IF dispatch.target == 'native.exec' → NATIVE_EXEC",
        "IF dispatch.target == 'native.main' → NATIVE_MAIN"
      ]
    },

    "HANDLER": {
      "native_runtime_handler": {
        "INPUT": "dispatch",
        "ACTIONS": [
          "validate dispatch packet",
          "select native execution mode",
          "spawn native process",
          "capture stdout/stderr",
          "serialize output",
          "update continuum_state.runtime.native"
        ],
        "OUTPUT": "native_runtime_result"
      }
    },

    "RESULT_FORMAT": {
      "FIELDS": {
        "timestamp": "iso8601",
        "mode": "exec | main",
        "exit_code": "integer",
        "stdout": "string",
        "stderr": "string"
      }
    },

    "ERRORS": {
      "INVALID_DISPATCH": "Dispatch packet missing required fields",
      "UNKNOWN_RUNTIME_MODE": "Dispatch target not mapped to native runtime",
      "NATIVE_EXEC_FAILURE": "Native binary returned non-zero exit code",
      "STATE_UPDATE_FAILURE": "Failed to update continuum state"
    },

    "META": {
      "CONTINUUM": "CBORD → CYBORD → CBORG → INCINTUUM",
      "LANGUAGES": [
        "C",
        "CPP",
        "JAVA",
        "ASM",
        "PHP"
      ]
    }
  }
}

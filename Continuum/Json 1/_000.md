{
  "INCINTUUM_CPP_RUNTIME_BRIDGE": {
    "VERSION": "1.0",
    "MODULE": "INCINTUUM_CPP_RUNTIME_BRIDGE.icrb",
    "ROLE": "Execution bridge between CBORG dispatch packets and C++ runtime",
    "DESCRIPTION": "Receives dispatch packets from INCINTUUM Gateway Router, invokes C++ binaries or shared objects, captures output, and updates continuum state.",

    "INPUT": "dispatch",
    "OUTPUT": "cpp_runtime_result",

    "RUNTIME_MAP": {
      "CPP_EXEC": {
        "FORMAT": {
          "binary": "string",
          "args": "array",
          "env": "object"
        },
        "DESC": "Direct execution of C++ binaries compiled by CYBORD or CBORD"
      },
      "CPP_SHARED": {
        "FORMAT": {
          "library": "string",
          "symbol": "string",
          "args": "array"
        },
        "DESC": "Execution of C++ shared library symbols"
      },
      "CPP_MAIN": {
        "FORMAT": {
          "binary": "string"
        },
        "DESC": "Execution of main entrypoint for C++ binaries"
      }
    },

    "EXECUTION_LOGIC": {
      "DESCRIPTION": "Maps dispatch packets to C++ execution modes.",
      "RULES": [
        "IF dispatch.target == 'cpp.exec' → CPP_EXEC",
        "IF dispatch.target == 'cpp.shared' → CPP_SHARED",
        "IF dispatch.target == 'cpp.main' → CPP_MAIN"
      ]
    },

    "HANDLER": {
      "cpp_runtime_handler": {
        "INPUT": "dispatch",
        "ACTIONS": [
          "validate dispatch packet",
          "select C++ execution mode",
          "spawn C++ process or load shared library",
          "capture stdout/stderr",
          "serialize output",
          "update continuum_state.runtime.cpp"
        ],
        "OUTPUT": "cpp_runtime_result"
      }
    },

    "RESULT_FORMAT": {
      "FIELDS": {
        "timestamp": "iso8601",
        "mode": "exec | shared | main",
        "exit_code": "integer",
        "stdout": "string",
        "stderr": "string"
      }
    },

    "ERRORS": {
      "INVALID_DISPATCH": "Dispatch packet missing required fields",
      "UNKNOWN_RUNTIME_MODE": "Dispatch target not mapped to C++ runtime",
      "CPP_EXEC_FAILURE": "C++ binary returned non-zero exit code",
      "CPP_SHARED_FAILURE": "Shared library symbol invocation failed",
      "STATE_UPDATE_FAILURE": "Failed to update continuum state"
    },

    "META": {
      "CONTINUUM": "CBORD → CYBORD → CBORG → INCINTUUM",
      "LANGUAGES": [
        "C",
        "CPP",
        "JAVA",
        "ASM",
        "PHP"
      ]
    }
  }
}
